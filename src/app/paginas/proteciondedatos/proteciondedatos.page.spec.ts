import { ComponentFixture, TestBed } from '@angular/core/testing';
import { ProteciondedatosPage } from './proteciondedatos.page';

describe('ProteciondedatosPage', () => {
  let component: ProteciondedatosPage;
  let fixture: ComponentFixture<ProteciondedatosPage>;

  beforeEach(() => {
    fixture = TestBed.createComponent(ProteciondedatosPage);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
